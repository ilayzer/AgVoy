{% extends 'baselayout.html.twig' %}

{% block title %}All rooms{% endblock %}

{% block body %}
    {#    {{ dump(rooms) }}#}
    <h1>Clement 1</h1>
    <p> Ceci est un premier paragraphe cependantg je suis quand meme censé faire un site assez serieux</p>
    <p>Ceci étant dit, Before doing anything, you need some ideas. What should your website actually do? A website can do basically anything, but, for your first try, you should keep things simple.
        We'll start by creating a simple webpage with a heading,
        an image, and a few paragraphs. <a href="https://startbootstrap.com/"> tesmortsau cmaps</a></p>
    <a>eruigfheirudvgerdufivgui</a>
    <table>
        <thead>
        <tr>
            <th>#</th>
            <th>Résumé</th>
            <th>Prix</th>
            <th>&nbsp;</th>
        </tr>
        </thead>
        <tbody>
        {% for room in rooms %}
            <tr>
                <td>{{ room.id }}</td>
                <td>{{ room.summary }}</td>
                <td>{{ room.price }}</td>
                <td><a href="{{ path('room_show', {id: room.id}) }}">Afficher</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('room_new') }}">Créer</a>
{% endblock %}
